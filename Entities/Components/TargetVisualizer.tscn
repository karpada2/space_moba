[gd_scene load_steps=8 format=3 uid="uid://b11b2nrejvoal"]

[ext_resource type="Script" uid="uid://d4mc8qb7ua02b" path="res://Entities/Components/TargetVisualizer.gd" id="1_d1hlm"]

[sub_resource type="Shader" id="Shader_nl50s"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform bool outline_active = true;
uniform bool tint_active = false;
uniform float tint_strength: hint_range(0, 1) = 0.5;
uniform vec4 tint_color : source_color = vec4(1.0, 0.0, 0.0, 1.0);

uniform vec4 outline_color : source_color = vec4(1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	if (outline_active) {
		vec2 uv = UV;
		
		if (add_margins) {
			vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
			
			uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
			
			if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
				COLOR.a = 0.0;
			} else {
				COLOR = texture(TEXTURE, uv);
			}
		} else {
			COLOR = texture(TEXTURE, uv);
		}
		
		if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
			COLOR.rgb = inside ? mix(COLOR.rgb, outline_color.rgb, outline_color.a) : outline_color.rgb;
			COLOR.a += (1.0 - COLOR.a) * outline_color.a;
		}
	}
	if (tint_active) {
		vec4 input_color = COLOR;
		COLOR = mix(input_color, tint_color, tint_strength);
		COLOR.a = input_color.a;
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_pordg"]
resource_local_to_scene = true
shader = SubResource("Shader_nl50s")
shader_parameter/outline_active = false
shader_parameter/tint_active = false
shader_parameter/tint_strength = 0.70000003325
shader_parameter/tint_color = Color(1, 0, 0, 1)
shader_parameter/outline_color = Color(0, 0, 0, 1)
shader_parameter/width = 0.250000011875
shader_parameter/pattern = 2
shader_parameter/inside = false
shader_parameter/add_margins = true

[sub_resource type="Shader" id="Shader_d1hlm"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform bool outline_active = true;
uniform bool tint_active = false;
uniform float tint_strength: hint_range(0, 1) = 0.5;
uniform vec4 tint_color : source_color = vec4(1.0, 0.0, 0.0, 1.0);

uniform vec4 outline_color : source_color = vec4(1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	if (outline_active) {
		vec2 uv = UV;
		
		if (add_margins) {
			vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
			
			uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
			
			if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
				COLOR.a = 0.0;
			} else {
				COLOR = texture(TEXTURE, uv);
			}
		} else {
			COLOR = texture(TEXTURE, uv);
		}
		
		if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
			COLOR.rgb = inside ? mix(COLOR.rgb, outline_color.rgb, outline_color.a) : outline_color.rgb;
			COLOR.a += (1.0 - COLOR.a) * outline_color.a;
		}
	}
	if (tint_active) {
		vec4 input_color = COLOR;
		COLOR = mix(input_color, tint_color, tint_strength);
		COLOR.a = input_color.a;
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_nl50s"]
resource_local_to_scene = true
shader = SubResource("Shader_d1hlm")
shader_parameter/outline_active = true
shader_parameter/tint_active = false
shader_parameter/tint_strength = 0.70000003325
shader_parameter/tint_color = Color(1, 0, 0, 1)
shader_parameter/outline_color = Color(0, 0, 0, 1)
shader_parameter/width = 0.250000011875
shader_parameter/pattern = 2
shader_parameter/inside = false
shader_parameter/add_margins = true

[sub_resource type="Shader" id="Shader_31g7v"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform bool outline_active = true;
uniform bool tint_active = false;
uniform float tint_strength: hint_range(0, 1) = 0.5;
uniform vec4 tint_color : source_color = vec4(1.0, 0.0, 0.0, 1.0);

uniform vec4 outline_color : source_color = vec4(1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	if (outline_active) {
		vec2 uv = UV;
		
		if (add_margins) {
			vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
			
			uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
			
			if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
				COLOR.a = 0.0;
			} else {
				COLOR = texture(TEXTURE, uv);
			}
		} else {
			COLOR = texture(TEXTURE, uv);
		}
		
		if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
			COLOR.rgb = inside ? mix(COLOR.rgb, outline_color.rgb, outline_color.a) : outline_color.rgb;
			COLOR.a += (1.0 - COLOR.a) * outline_color.a;
		}
	}
	if (tint_active) {
		vec4 input_color = COLOR;
		COLOR = mix(input_color, tint_color, tint_strength);
		COLOR.a = input_color.a;
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_42fjn"]
resource_local_to_scene = true
shader = SubResource("Shader_31g7v")
shader_parameter/outline_active = true
shader_parameter/tint_active = false
shader_parameter/tint_strength = 0.70000003325
shader_parameter/tint_color = Color(1, 0, 0, 1)
shader_parameter/outline_color = Color(0, 0, 0, 1)
shader_parameter/width = 0.250000011875
shader_parameter/pattern = 2
shader_parameter/inside = false
shader_parameter/add_margins = true

[node name="TargetVisualizer" type="CanvasLayer"]
follow_viewport_enabled = true
script = ExtResource("1_d1hlm")

[node name="LineVisualizer" type="Line2D" parent="."]
material = SubResource("ShaderMaterial_pordg")

[node name="PositionVisualizer" type="Sprite2D" parent="."]
material = SubResource("ShaderMaterial_nl50s")
scale = Vector2(8, 8)

[node name="EntityVisualizer" type="Sprite2D" parent="."]
material = SubResource("ShaderMaterial_42fjn")
scale = Vector2(8, 8)
